<section class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
  <div>
    <h1 class="text-4xl md:text-5xl font-extrabold text-sky-800 leading-tight">{{ title|default('Welcome to My Hotel') }}</h1>
    <p class="mt-4 text-lg text-slate-700">{{ subtitle|default('Comfort, style and unforgettable experiences in the heart of the city.') }}</p>

    <div class="mt-8 flex items-center gap-4">
      <a href="#booking" class="inline-flex items-center px-6 py-3 bg-sky-600 text-white rounded-lg shadow hover:bg-sky-700">{{ cta_text|default('Book now') }}</a>
      <a href="#rooms" class="inline-flex items-center px-6 py-3 border border-sky-600 text-sky-600 rounded-lg hover:bg-sky-50">Explore rooms</a>
    </div>
  </div>

  <div class="rounded-xl overflow-hidden shadow-lg">
    <img src="{{ image_url|default('https://images.unsplash.com/photo-1618773928121-c32242e63f39?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D') }}" alt="Hero" class="w-full h-80 object-cover" loading="Lazy">
  </div>
</section>
